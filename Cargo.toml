[package]
name = "idc"
version = "0.1.0"
edition = "2024"
description = "A lightweight crate for propagating errors."
readme = "README.md"
license = "MIT"
repository = "https://github.com/Cheese-Space/idc"

[dependencies]

[features]
default = ["std"]
std = []